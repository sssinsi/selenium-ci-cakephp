machine:
  timezone: Asia/Tokyo
  hosts:
    localhost: 127.0.0.1
  php:
   version: 5.3.3
#environment:

dependencies:
  pre:
    - sudo pip install --upgrade pip
    - sudo pip install selenium
    - cp ~/selenium-ci-cakephp/ci_test/apache_setting /etc/apache2/sites-available
    - a2ensite apache_setting
    - sudo service apache2 restart

  post:
    - echo "<?php
      class DATABASE_CONFIG {
        var $test = array(
          'driver' => 'mysql',
          'persistent' => false,
          'host' => '127.0.0.1',
          'login' => 'ubuntu',
          'password' => '',
          'database' => 'test_circle_ci',
          'prefix' => '',
          'encoding' => 'utf8',
      }" > app/config/database.php
#  override:
#
#  pre:
#

database:
  override:
    - mysql -u ubuntu circle_test < ~/ci_test/test_setup.sql


test:
  pre:
    - echo "-----start test-----"
#  override:
#    - python
  post:
    - echo "-----end test-----"
